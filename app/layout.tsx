import type { Metadata } from "next";
import localFont from 'next/font/local';
import "../styles/globals.css";

const cosmata = localFont({
  src: [
    { path: '../public/fonts/Fontspring-DEMO-cosmata-regular.woff2', weight: '400', style: 'normal' },
    { path: '../public/fonts/Fontspring-DEMO-cosmata-medium.woff2', weight: '500', style: 'normal' },
    { path: '../public/fonts/Fontspring-DEMO-cosmata-bold.woff2', weight: '700', style: 'normal' },
  ],
  variable: '--font-cosmata',
  display: 'swap',
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${cosmata.variable} antialiased`} suppressHydrationWarning>
      <head><meta name="view-transition" content="same-origin" /></head>
      <body className="transition-colors duration-300">{children}</body>
    </html>
  );
}
